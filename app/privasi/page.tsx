import { LegalDocument } from '@/components/LegalDocument';
import { privasiRetensiContent } from '@/lib/legal';

export default function PrivasiPage() {
  return <LegalDocument content={privasiRetensiContent} />;
}
